version: "1"

# BASE DE DATOS (sección aparte, NO dentro de services)
databases:
  - name: reclutamiento-db
    databaseName: reclutamiento_db
    user: reclutamiento_user
    plan: free
    region: oregon

# SERVICIOS (solo aplicaciones web, workers, etc.)
services:
  - type: web
    name: reclutamiento-web
    runtime: python
    plan: free
    region: oregon
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"  # Mejor que python app.py para producción
    healthCheckPath: /api/health  # Importante para que Render sepa que está listo
    
    # COMANDO CLAVE: Ejecuta el seed antes de iniciar la app
    preDeployCommand: "python init_produccion.py"
    
    envVars:
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true
      - key: FLASK_DEBUG
        value: "false"
      - key: SESSION_COOKIE_SECURE
        value: "true"
      
      # Conexión automática a la base de datos
      - key: DATABASE_URL
        fromDatabase:
          name: reclutamiento-db
          property: connectionString
      
      # Variable para que el script sepa que está en Render
      - key: RENDER
        value: "true"